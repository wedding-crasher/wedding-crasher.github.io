---
interface CategoryLink {
  label: string;
  slug: string;
  count: number;
}

interface Props {
  categories: CategoryLink[];
}

const { categories } = Astro.props;
---

<aside class="sidebar-stack">
  <section class="sidebar-card profile-card">
    <img
      class="profile-card__image"
      src="/images/profile-placeholder.svg"
      alt="Profile placeholder"
      width="320"
      height="240"
    />
    <p class="sidebar-label">About me</p>
    <h2>Hangwoo Cho</h2>
    <p>
      I write about real operating issues in ML and LLM systems from the perspective of a
      Software Solution Engineer. This sidebar acts as a compact profile and contact surface.
    </p>
    <div class="profile-card__meta">
      <span>Seoul</span>
      <span>Open to technical writing</span>
    </div>
    <div class="profile-card__links">
      <a class="text-link" href="/about/">Profile</a>
      <a class="text-link" href="/">Archive</a>
    </div>
  </section>

  <section class="sidebar-card compact-card">
    <p class="sidebar-label">Focus areas</p>
    <ul class="focus-list">
      <li>Machine Learning Engineer</li>
      <li>LLM Engineer</li>
      <li>MLOps</li>
      <li>Applied AI</li>
    </ul>
  </section>

  <section class="sidebar-card compact-card">
    <p class="sidebar-label">Categories</p>
    <div class="category-list">
      {
        categories.map((category) => (
          <a class="category-pill" href={`/blog/category/${category.slug}/`}>
            <span>{category.label}</span>
            <strong>{category.count}</strong>
          </a>
        ))
      }
    </div>
  </section>

  <section class="sidebar-card compact-card accent-card">
    <p class="sidebar-label">Positioning</p>
    <p>
      The goal is to leave behind the problem, the reasoning, and the trade-off, not just the outcome.
    </p>
    <a class="text-link" href="/about/">View profile details</a>
  </section>
</aside>