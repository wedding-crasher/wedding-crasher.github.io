---
interface CategoryLink {
  label: string;
  slug: string;
  count: number;
}

interface Props {
  categories: CategoryLink[];
}

const { categories } = Astro.props;
---

<aside class="sidebar-stack">
  <section class="sidebar-card profile-card">
    <img
      class="profile-card__image"
      src="/images/profile-placeholder.svg"
      alt="Profile placeholder"
      width="320"
      height="240"
    />
    <p class="sidebar-label">About me</p>
    <h2>Hangwoo Cho</h2>
    <p>
      I am a Solution Engineer on Microsoft's AI Build Team in Korea, working on enterprise
      AI and ML problems. I write about the technical and business challenges that show up when
      teams try to make those systems work in practice.
    </p>
    <div class="profile-card__meta">
      <span>Korea</span>
      <span>Microsoft AI Build Team</span>
    </div>
    <div class="profile-card__links">
      <a class="text-link" href="/about/">Profile</a>
      <a class="text-link" href="/">Archive</a>
    </div>
  </section>

  <section class="sidebar-card compact-card">
    <p class="sidebar-label">Focus areas</p>
    <ul class="focus-list">
      <li>Enterprise AI adoption</li>
      <li>Applied machine learning</li>
      <li>LLM systems</li>
      <li>Solution architecture</li>
    </ul>
  </section>

  <section class="sidebar-card compact-card">
    <p class="sidebar-label">Categories</p>
    <div class="category-list">
      {
        categories.map((category) => (
          <a class="category-pill" href={`/blog/category/${category.slug}/`}>
            <span>{category.label}</span>
            <strong>{category.count}</strong>
          </a>
        ))
      }
    </div>
  </section>

  <section class="sidebar-card compact-card accent-card">
    <p class="sidebar-label">Positioning</p>
    <p>
      This blog focuses on how enterprise AI and ML problems are framed, solved, and communicated,
      not just how the final system looks after the fact.
    </p>
    <a class="text-link" href="/about/">View profile details</a>
  </section>
</aside>